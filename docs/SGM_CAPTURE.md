# SGM traffic capture — the 20-minute operator task

Same-game-multi pricing lives behind session-authenticated endpoints that
anonymous discovery can't reach. One proxy session through the betting apps
hands us the endpoints; after that, SGM EV scanning is a normal build.

## One-time setup (Mac)

```bash
brew install mitmproxy
mitmweb --listen-port 8080   # opens